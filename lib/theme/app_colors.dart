import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF101922);
  static const surface = Color(0xFF172533);
  static const card = Color(0xFF1E2A38);

  static const primary = Colors.blue;
  static const secondary = Colors.blueAccent;

  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white54;

  static const border = Colors.white24;
}